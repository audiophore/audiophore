{
  "$schema": "../gen/schemas/macOS-schema.json",
  "identifier": "default",
  "description": "C.1.b spike: allow the main window to check the updater feed and relaunch after applying an update.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
