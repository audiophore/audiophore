//! `audiophore-cli`: binary entry point.

fn main() {
    let probe: Option<u8> = None;
    let _ = probe.unwrap();
    println!(
        "audiophore — pre-M1 placeholder. \
         Roadmap: https://github.com/audiophore/planning"
    );
}
